spring:
  application:
    name: candy
  datasource:
    # MySQL configuration
    url: jdbc:postgresql://ep-cool-sea-a1p9qnk3-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_5beEtxgY8SpN
    driverClassName: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME:haminhan234@gmail.com}
    password: ${EMAIL_PASSWORD:qvwgcifelrfeloxc}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
# CORS allowed origins (comma-separated)
app:
  cors:
    allowed-origins: http://localhost:3000
  jwt:
    secret: bXlTdXBlclNlY3VyZUp3dFNlY3JldEtleUZvckNhbmR5U2hvcEFwcGxpY2F0aW9uMTIzNDU2Nzg5MA==
    expiration: 86400000 # 24 hours in milliseconds
    issuer: candy-book-store
  admin:
    username: admin
    password: admin123
    email: admin@example.com

server:
  port: 8080

# Configure logging
logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    com.example.candy.security: DEBUG
    com.example.candy.security.jwt: DEBUG
    com.example.candy.controllers: DEBUG
    com.example.candy.services.EmailService: DEBUG
    com.example.candy.services.OrderService: DEBUG
    org.springframework.mail: DEBUG
    com.sun.mail: DEBUG

# Actuator configuration for health checks
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized
